{
  "version": 2,
  "builds": [
    {
      "src": "start.sh",
      "use": "@vercel/static-build",
      "config": { "maxLambdaSize": "15mb" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "start.sh" }
  ]
}
